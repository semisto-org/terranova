# Diagramme de Flux - Intégration Modales Academy

## 🎯 Vue d'ensemble du système

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         Academy/Index.jsx                                │
│                         (893 lignes)                                     │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │    useState + useCallback     │
                    └───────────────┬───────────────┘
                                    │
        ┌───────────────────────────┼───────────────────────────┐
        │                           │                           │
        ▼                           ▼                           ▼
┌───────────────┐          ┌───────────────┐          ┌───────────────┐
│  activeModal  │          │   modalData   │          │  8 Handlers   │
│   (string)    │          │   (object)    │          │  (functions)  │
└───────┬───────┘          └───────┬───────┘          └───────┬───────┘
        │                          │                          │
        │     ┌────────────────────┴────────────────────┐    │
        │     │                                          │    │
        ▼     ▼                                          ▼    ▼
┌──────────────────────────────────────────────────────────────────────┐
│                    Rendu conditionnel des modales                    │
│                                                                      │
│  {activeModal === 'training' && <TrainingFormModal ... />}          │
│  {activeModal === 'session' && <SessionFormModal ... />}            │
│  {activeModal === 'registration' && <RegistrationFormModal ... />}  │
│  {activeModal === 'paymentStatus' && <PaymentStatusModal ... />}    │
│  {activeModal === 'document' && <DocumentFormModal ... />}          │
│  {activeModal === 'checklistItem' && <ChecklistItemModal ... />}    │
│  {activeModal === 'expense' && <ExpenseFormModal ... />}            │
│  {activeModal === 'ideaNote' && <IdeaNoteFormModal ... />}          │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flux d'interaction utilisateur

### Exemple: Créer une formation

```
┌──────────────┐
│  Utilisateur │
└──────┬───────┘
       │ Clic sur "Nouvelle formation"
       ▼
┌─────────────────────────────────────────────┐
│  actions.createTraining()                   │
│                                             │
│  setModalData({ isEdit: false })           │
│  setActiveModal('training')                │
└─────────────┬───────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────┐
│  TrainingFormModal s'affiche                │
│                                             │
│  Props:                                     │
│  - training: null (create mode)            │
│  - trainingTypes: [...data.trainingTypes]  │
│  - onSubmit: handleTrainingSubmit          │
│  - onCancel: () => close modal             │
│  - busy: false                             │
└─────────────┬───────────────────────────────┘
              │
              │ Utilisateur remplit le formulaire
              │
              ▼
┌─────────────────────────────────────────────┐
│  Utilisateur clique "Créer la formation"    │
└─────────────┬───────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────┐
│  handleTrainingSubmit(values)               │
│                                             │
│  await runMutation(() =>                    │
│    apiRequest('/api/v1/academy/trainings', │
│      { method: 'POST', body: values }      │
│    )                                        │
│  )                                          │
└─────────────┬───────────────────────────────┘
              │
              ├─── Succès ────▶ setActiveModal(null)
              │                 setModalData(null)
              │                 loadAcademy() (refresh)
              │
              └─── Erreur ────▶ setError(message)
                                (modale reste ouverte)
```

---

## 🗂️ Structure des composants

```
app/frontend/
│
├── pages/
│   └── Academy/
│       └── Index.jsx ◄────────────────────┐
│           │                               │
│           │ imports                       │
│           ▼                               │
│   ┌─────────────────────────────┐        │
│   │   @/components/academy      │        │
│   └─────────────┬───────────────┘        │
│                 │                         │
│                 ▼                         │
└── components/                             │
    └── academy/                            │
        ├── index.js ◄──────────────────────┤
        │   │                               │
        │   │ exports                       │
        │   ▼                               │
        ├── TrainingFormModal.jsx           │
        │   ├── useState (form state)       │
        │   ├── validation logic            │
        │   └── render form                 │
        │                                   │
        ├── SessionFormModal.jsx            │
        │   ├── multi-select logic          │
        │   ├── date validation             │
        │   └── render form                 │
        │                                   │
        ├── RegistrationFormModal.jsx       │
        │   ├── email validation            │
        │   ├── amount validation           │
        │   └── render form                 │
        │                                   │
        ├── PaymentStatusModal.jsx          │
        │   ├── status logic                │
        │   ├── amount validation           │
        │   └── render form                 │
        │                                   │
        ├── DocumentFormModal.jsx           │
        │   ├── URL validation              │
        │   ├── type selection              │
        │   └── render form                 │
        │                                   │
        ├── ChecklistItemModal.jsx          │
        │   ├── simple text input           │
        │   └── render form                 │
        │                                   │
        ├── ExpenseFormModal.jsx            │
        │   ├── category selection          │
        │   ├── amount validation           │
        │   └── render form                 │
        │                                   │
        └── IdeaNoteFormModal.jsx           │
            ├── tags parsing                │
            ├── content textarea            │
            └── render form                 │
```

---

## 🔀 Flux de données (Data Flow)

```
                         ┌──────────────────┐
                         │  Rails Backend   │
                         │  (API v1)        │
                         └────────┬─────────┘
                                  │
                         GET /api/v1/academy
                                  │
                                  ▼
                         ┌──────────────────┐
                         │   loadAcademy()  │
                         │   setData(...)   │
                         └────────┬─────────┘
                                  │
                    ┌─────────────┴─────────────┐
                    │                           │
                    ▼                           ▼
          ┌──────────────────┐        ┌──────────────────┐
          │  data.trainings  │        │ data.members     │
          │  data.sessions   │        │ data.locations   │
          │  data.expenses   │        │ data.types       │
          │  ...             │        │ ...              │
          └────────┬─────────┘        └────────┬─────────┘
                   │                           │
                   └─────────────┬─────────────┘
                                 │
                    Props passées aux modales
                                 │
                   ┌─────────────┴─────────────┐
                   │                           │
                   ▼                           ▼
        ┌─────────────────────┐    ┌─────────────────────┐
        │ TrainingFormModal   │    │  SessionFormModal   │
        │                     │    │                     │
        │ trainingTypes={...} │    │ locations={...}     │
        │                     │    │ members={...}       │
        └─────────┬───────────┘    └─────────┬───────────┘
                  │                          │
                  │ onSubmit(values)         │ onSubmit(values)
                  │                          │
                  ▼                          ▼
        ┌─────────────────────────────────────────────┐
        │         handleTrainingSubmit()              │
        │         handleSessionSubmit()               │
        │                                             │
        │  await runMutation(() =>                    │
        │    apiRequest(endpoint, { method, body })  │
        │  )                                          │
        └─────────┬───────────────────────────────────┘
                  │
                  │ POST/PATCH /api/v1/academy/*
                  │
                  ▼
        ┌──────────────────────────────────────────┐
        │  Rails Backend traite la requête         │
        │  Sauvegarde en DB                        │
        │  Retourne JSON                           │
        └─────────┬────────────────────────────────┘
                  │
                  ▼
        ┌──────────────────────────────────────────┐
        │  if (success) {                          │
        │    setActiveModal(null)                  │
        │    loadAcademy() // refresh              │
        │  }                                       │
        └──────────────────────────────────────────┘
```

---

## 🎨 Architecture des modales (Pattern commun)

```
┌─────────────────────────────────────────────────────────────────┐
│                      Modal Component                            │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  Backdrop (z-50, bg-black/20, backdrop-blur-sm)          │ │
│  │                                                           │ │
│  │  ┌─────────────────────────────────────────────────────┐ │ │
│  │  │  Modal Container (max-w-2xl, bg-white, rounded-2xl)│ │ │
│  │  │                                                     │ │ │
│  │  │  ┌──────────────────────────────────────────────┐  │ │ │
│  │  │  │  Header                                      │  │ │ │
│  │  │  │  - Titre (h2, text-2xl)                     │  │ │ │
│  │  │  │  - Description (text-stone-600)             │  │ │ │
│  │  │  │  - Bouton Close (X)                         │  │ │ │
│  │  │  └──────────────────────────────────────────────┘  │ │ │
│  │  │                                                     │ │ │
│  │  │  ┌──────────────────────────────────────────────┐  │ │ │
│  │  │  │  Form Content (p-6, space-y-5)              │  │ │ │
│  │  │  │                                              │  │ │ │
│  │  │  │  ┌────────────────────────────────────────┐ │  │ │ │
│  │  │  │  │  Field 1 (Label + Input + Error)      │ │  │ │ │
│  │  │  │  └────────────────────────────────────────┘ │  │ │ │
│  │  │  │  ┌────────────────────────────────────────┐ │  │ │ │
│  │  │  │  │  Field 2 (Label + Input + Error)      │ │  │ │ │
│  │  │  │  └────────────────────────────────────────┘ │  │ │ │
│  │  │  │  ┌────────────────────────────────────────┐ │  │ │ │
│  │  │  │  │  Field N ...                          │ │  │ │ │
│  │  │  │  └────────────────────────────────────────┘ │  │ │ │
│  │  │  └──────────────────────────────────────────────┘  │ │ │
│  │  │                                                     │ │ │
│  │  │  ┌──────────────────────────────────────────────┐  │ │ │
│  │  │  │  Footer (flex gap-3, justify-end)           │  │ │ │
│  │  │  │                                              │  │ │ │
│  │  │  │  ┌──────────────┐  ┌──────────────────────┐│  │ │ │
│  │  │  │  │ Annuler      │  │  Soumettre           ││  │ │ │
│  │  │  │  │ (gray)       │  │  (Academy #B01A19)   ││  │ │ │
│  │  │  │  └──────────────┘  └──────────────────────┘│  │ │ │
│  │  │  └──────────────────────────────────────────────┘  │ │ │
│  │  └─────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘

Interactions:
- Clic backdrop → onCancel()
- Escape key → onCancel()
- Clic X → onCancel()
- Clic Annuler → onCancel()
- Clic Soumettre → onSubmit(values)
- Enter key (focus input) → onSubmit(values)
```

---

## 🔧 État des modales (State Management)

```
Academy/Index.jsx State:

┌─────────────────────────────────────────────────────────┐
│  const [activeModal, setActiveModal] = useState(null)   │
│                                                         │
│  Valeurs possibles:                                     │
│  - null (aucune modale)                                │
│  - 'training'                                          │
│  - 'session'                                           │
│  - 'registration'                                      │
│  - 'paymentStatus'                                     │
│  - 'document'                                          │
│  - 'checklistItem'                                     │
│  - 'expense'                                           │
│  - 'ideaNote'                                          │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  const [modalData, setModalData] = useState(null)       │
│                                                         │
│  Structure selon le contexte:                           │
│                                                         │
│  Create mode:                                          │
│  {                                                     │
│    isEdit: false,                                      │
│    trainingId: 123,                                    │
│    trainingPrice: 180,                                 │
│    ...                                                 │
│  }                                                     │
│                                                         │
│  Edit mode:                                            │
│  {                                                     │
│    isEdit: true,                                       │
│    training: { id, title, ... },                       │
│    session: { id, startDate, ... },                    │
│    expense: { id, amount, ... },                       │
│    ...                                                 │
│  }                                                     │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  const [busy, setBusy] = useState(false)                │
│                                                         │
│  Géré par runMutation():                                │
│  - setBusy(true) avant l'API call                      │
│  - setBusy(false) après succès/erreur                  │
│  - Passé aux modales via prop `busy`                   │
│  - Désactive les boutons pendant la soumission         │
└─────────────────────────────────────────────────────────┘
```

---

## 📱 Responsive Design

```
Mobile (< 640px)                Tablet (640-1024px)              Desktop (> 1024px)
────────────────                ───────────────────              ──────────────────

┌────────────┐                  ┌──────────────────┐            ┌──────────────────────┐
│ Modal      │                  │                  │            │                      │
│ Full-screen│                  │     Modal        │            │       Backdrop       │
│            │                  │   (centered)     │            │     (blur effet)     │
│            │                  │                  │            │                      │
│ [Titre]    │                  │   ┌──────────┐   │            │   ┌──────────────┐   │
│            │                  │   │          │   │            │   │              │   │
│ [Champs]   │                  │   │ [Champs] │   │            │   │   [Champs]   │   │
│            │                  │   │          │   │            │   │              │   │
│            │                  │   └──────────┘   │            │   └──────────────┘   │
│ [Boutons]  │                  │                  │            │                      │
│            │                  │                  │            │                      │
└────────────┘                  └──────────────────┘            └──────────────────────┘

max-w-full                      max-w-2xl                        max-w-2xl
h-screen                        auto                             auto
rounded-none                    rounded-2xl                      rounded-2xl
p-4                            p-6                              p-6
```

---

## 🎯 Mapping Actions → Modales

```
actions.createTraining()        → TrainingFormModal (create)
        ↓
        setModalData({ isEdit: false })
        setActiveModal('training')

actions.editTraining(id)        → TrainingFormModal (edit)
        ↓
        setModalData({ isEdit: true, training: {...} })
        setActiveModal('training')

actions.addSession(trainingId)  → SessionFormModal (create)
        ↓
        setModalData({ isEdit: false, trainingId })
        setActiveModal('session')

actions.editSession(sessionId)  → SessionFormModal (edit)
        ↓
        setModalData({ isEdit: true, session: {...} })
        setActiveModal('session')

...etc (13 actions au total)
```

---

## 🔄 Cycle de vie d'une modale

```
1. OUVERTURE
   ┌────────────────────────────────────────────┐
   │ User action (button click)                 │
   └──────────────┬─────────────────────────────┘
                  │
                  ▼
   ┌────────────────────────────────────────────┐
   │ setModalData({ ... })                      │
   │ setActiveModal('modalName')                │
   └──────────────┬─────────────────────────────┘
                  │
                  ▼
   ┌────────────────────────────────────────────┐
   │ Modal component renders                    │
   │ - Auto-focus premier champ                 │
   │ - Initialise form state                    │
   │ - Écoute Escape key                        │
   └──────────────┬─────────────────────────────┘
                  │
                  ▼
2. INTERACTION
   ┌────────────────────────────────────────────┐
   │ User remplit les champs                    │
   │ - Validation en temps réel                 │
   │ - Messages d'erreur affichés               │
   └──────────────┬─────────────────────────────┘
                  │
                  ▼
3. SOUMISSION
   ┌────────────────────────────────────────────┐
   │ onSubmit(values)                           │
   │ → handleXSubmit(values)                    │
   └──────────────┬─────────────────────────────┘
                  │
                  ▼
   ┌────────────────────────────────────────────┐
   │ runMutation(() => apiRequest(...))         │
   │ - setBusy(true)                            │
   │ - Désactive boutons                        │
   └──────────────┬─────────────────────────────┘
                  │
            ┌─────┴─────┐
            │           │
            ▼           ▼
    ┌───────────┐  ┌───────────┐
    │  Succès   │  │  Erreur   │
    └─────┬─────┘  └─────┬─────┘
          │              │
          ▼              ▼
4. FERMETURE         RESTE OUVERTE
   ┌────────────────┐  ┌─────────────────┐
   │ setActiveModal │  │ setError(msg)   │
   │ (null)         │  │ setBusy(false)  │
   │ setModalData   │  │                 │
   │ (null)         │  │ User peut retry │
   │ loadAcademy()  │  └─────────────────┘
   └────────────────┘
```

---

## 📊 Comparaison Avant/Après

### AVANT (window.prompt)

```
User clique bouton
        ↓
window.prompt("Question 1")
        ↓
window.prompt("Question 2")
        ↓
window.prompt("Question 3")
        ↓
API call direct
        ↓
Refresh data
```

**Problèmes**:
- ❌ UX médiocre (prompts natifs)
- ❌ Pas de validation
- ❌ Un champ à la fois
- ❌ Pas d'accessibilité
- ❌ Pas responsive

### APRÈS (Modales React)

```
User clique bouton
        ↓
setModalData() + setActiveModal()
        ↓
Modal s'ouvre avec animation
        ↓
User remplit formulaire complet
(validation temps réel)
        ↓
onSubmit() → handler → runMutation()
        ↓
API call avec gestion d'état
        ↓
Succès → fermeture + refresh
Erreur → message + modale reste ouverte
```

**Avantages**:
- ✅ UX moderne et élégante
- ✅ Validation en temps réel
- ✅ Formulaire multi-champs
- ✅ Accessibilité complète
- ✅ Design responsive

---

**Date de création**: 2026-02-16
**Version**: 1.0.0
**Status**: ✅ Documentation complète
