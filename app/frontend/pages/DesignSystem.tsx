// bm-design-system: reference page (route /design-system → app#design_system)
import { DesignSystem } from "@/components/design-system/DesignSystem";

export default function DesignSystemPage() {
  return <DesignSystem />;
}
